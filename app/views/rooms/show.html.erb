<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @room.name %>
</p>

<p>
  <strong>Vonage session:</strong>
  <%= @room.vonage_session_id %>
</p>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>

<div  id="videos"
      data-controller="vonage"
      data-api-key-"<%= Rails.application.credentials.vonage_api_key %>"
      data-vonage-session-id="<%= @vonage.session_id %>"
      data-vonage-token="<%= @token %>"
      data-vonage-name="<%= current_user.name %>"
      >
</div>