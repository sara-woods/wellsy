
<!-- div 1  -->


<div class="container">
  <div class="row">
    <div class="col chhead">
      <h1>Checkout</h1>
      <h2>Get Ready to change your life</h2>

    </div>
  </div>
</div>




<!-- div 2  -->


<div class="container mb-5 mt-2">
  <div class="row" id="index-sessions">
    <%= render 'sessions/session_trainee', session: @order.session, upcoming: false, past: false %>
  </div>
</div>




<!-- div 3 -->

<div class="col chhead">
  <div class="container ch">
    <p>Cart total: <strong><%= humanized_money_with_symbol @order.amount %></strong></p>
    <button id="pay" class="btn btn-primary">Pay</button>
    <script src="https://js.stripe.com/v3/"></script>
    <script>
      const paymentButton = document.getElementById('pay');
      paymentButton.addEventListener('click', () => {
      const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
      stripe.redirectToCheckout({
      sessionId: '<%= @order.checkout_session_id %>'
      });
      });
    </script>
  </div>
</div>
